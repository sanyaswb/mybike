{
  "name": "test",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel --open",
    "build": "rm -rf dist .parcel-cache && parcel build src/index.html --public-url ./",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.1",
    "parcel": "latest"
  },
  "dependencies": {
    "acorn": "^8.15.0",
    "acorn-globals": "^7.0.1",
    "acorn-walk": "^8.3.4",
    "glob": "^11.0.3",
    "sharp": "^0.34.5"
  }
}
